# 通义千问 API Key
# 获取地址: https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=sk-your-dashscope-api-key-here

# 智能上下文配置
# 单文件最大Token数（默认4000）
MAX_TOKENS_PER_FILE=4000

# 上下文窗口大小（默认20行）
CONTEXT_WINDOW_SIZE=20

# 上下文策略（auto/aggressive/conservative）
# - auto: 自动选择最优策略（推荐）
# - aggressive: 激进压缩，最大化节省token
# - conservative: 保守策略，保留更多上下文
CONTEXT_STRATEGY=auto

# 是否启用缓存（true/false）
ENABLE_SMART_CACHE=true

# API相关配置
# 后端API基础地址（用于上传报告和获取项目信息）
AI_CR_API_BASE_URL=http://localhost:36788

# 测试环境调用长城接口的API_KEY
API_KEY_TEST=your-test-api-key

# 生产环境调用长城接口的API_KEY
API_KEY_PROD=your-prod-api-key